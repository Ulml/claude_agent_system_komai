{
  "name": "template-lm-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Template_LM — Browser-based AI Operating System: a meta-chat environment of persistent autonomous agents orchestrated end-to-end (LangGraph / LangChain / Pydantic, Firebase backend).",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/web",
    "build": "npm run build --workspace apps/web",
    "preview": "npm run preview --workspace apps/web",
    "typecheck": "npm run typecheck --workspace apps/web"
  }
}
